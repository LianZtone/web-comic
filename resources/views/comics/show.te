@extends('layouts.app', [
    'title' => $comic['title'].' | Velmics',
    'description' => $comic['summary'],
])

@section('content')
    @php
        $chapters = collect($comic['chapters'])->sortByDesc('number')->values();
    @endphp

    <section class="space-y-6">
        <article class="relative overflow-hidden rounded-[2rem] border border-base-300/70 bg-base-100/70 shadow-xl">
            <img src="{{ $comic['banner'] }}" alt="{{ $comic['title'] }} banner" class="h-[320px] w-full object-cover opacity-80 sm:h-[380px] xl:h-[420px]" fetchpriority="high" decoding="async">
            <div class="absolute inset-0 bg-gradient-to-tr from-black via-black/75 to-black/15"></div>

            <div class="relative z-10 p-5 sm:p-7">
                <div class="breadcrumbs text-sm text-white/70">
                    <ul>
                        <li><a href="{{ route('home') }}">Home</a></li>
                        <li><a href="{{ route('comics.index') }}">Library</a></li>
                        <li>{{ $comic['title'] }}</li>
                    </ul>
                </div>

                <div class="mt-5 grid gap-5 xl:grid-cols-[220px_minmax(0,1fr)_280px]">
                    <div class="mx-auto w-full max-w-[220px] xl:mx-0">
                        <img src="{{ $comic['cover'] }}" alt="{{ $comic['title'] }} cover" class="aspect-[4/5] w-full rounded-[1.75rem] border border-white/15 object-cover shadow-xl" decoding="async">
                    </div>

                    <div class="space-y-4">
                        <div class="flex flex-wrap items-center gap-2">
                            <span class="badge badge-primary border-0">{{ $comic['status'] }}</span>
                            <span class="badge badge-outline border-white/20 text-white">{{ $comic['latest_chapter']['label'] }}</span>
                            <span class="badge badge-outline border-white/20 text-white">{{ $comic['year'] }}</span>
                        </div>

                        <div>
                            @if (! empty($comic['subtitle']))
                                <div class="text-sm uppercase tracking-[0.28em] text-white/55">{{ $comic['subtitle'] }}</div>
                            @endif
                            <h1 class="mt-2 max-w-3xl text-3xl font-bold leading-tight text-white sm:text-5xl">{{ $comic['title'] }}</h1>
                            <p class="mt-3 max-w-3xl text-sm leading-7 text-white/75 sm:text-base">{{ $comic['summary'] }}</p>
                        </div>

                        <div class="grid gap-3 sm:grid-cols-3">
                            <div class="rounded-[1.35rem] border border-white/10 bg-black/25 p-4 backdrop-blur-sm">
                                <div class="text-xs uppercase tracking-[0.24em] text-white/45">Rating</div>
                                <div class="mt-2 text-2xl font-semibold text-white">{{ $comic['rating'] }}</div>
                                <div class="text-sm text-white/55">Skor pembaca</div>
                            </div>
                            <div class="rounded-[1.35rem] border border-white/10 bg-black/25 p-4 backdrop-blur-sm">
                                <div class="text-xs uppercase tracking-[0.24em] text-white/45">Chapter</div>
                                <div class="mt-2 text-2xl font-semibold text-white">{{ $comic['chapter_total'] }}</div>
                                <div class="text-sm text-white/55">Bab tersedia</div>
                            </div>
                            <div class="rounded-[1.35rem] border border-white/10 bg-black/25 p-4 backdrop-blur-sm">
                                <div class="text-xs uppercase tracking-[0.24em] text-white/45">Pages</div>
                                <div class="mt-2 text-2xl font-semibold text-white">{{ $comic['page_total'] }}</div>
                                <div class="text-sm text-white/55">Total halaman</div>
                            </div>
                        </div>

                        <div class="flex flex-wrap gap-3">
                            <a href="{{ route('chapters.show', ['slug' => $comic['slug'], 'chapter' => $comic['first_chapter']['number']]) }}" class="btn btn-primary rounded-2xl">
                                Mulai dari Awal
                            </a>
                            <a href="{{ route('chapters.show', ['slug' => $comic['slug'], 'chapter' => $comic['latest_chapter']['number']]) }}" class="btn btn-ghost rounded-2xl border border-white/15 bg-black/20 text-white hover:bg-black/35">
                                Chapter Terbaru
                            </a>
                        </div>
                    </div>

                    <div class="rounded-[1.75rem] border border-white/10 bg-black/25 p-5 text-white backdrop-blur-sm">
                        <div class="text-xs uppercase tracking-[0.28em] text-white/45">Spotlight</div>
                        <h2 class="mt-2 text-2xl font-semibold">{{ $comic['latest_chapter']['label'] }}</h2>
                        <p class="mt-2 text-sm text-white/70">{{ $comic['latest_chapter']['title'] }}</p>

                        <div class="mt-5 space-y-3 text-sm text-white/70">
                            <div class="flex items-center justify-between gap-3 rounded-2xl border border-white/10 bg-black/20 px-4 py-3">
                                <span>Rilis</span>
                                <span class="font-medium text-white">{{ $comic['latest_chapter']['release_label'] }}</span>
                            </div>
                            <div class="flex items-center justify-between gap-3 rounded-2xl border border-white/10 bg-black/20 px-4 py-3">
                                <span>Jadwal</span>
                                <span class="font-medium text-white">{{ $comic['schedule'] }}</span>
                            </div>
                            <div class="flex items-center justify-between gap-3 rounded-2xl border border-white/10 bg-black/20 px-4 py-3">
                                <span>Readers</span>
                                <span class="font-medium text-white">{{ $comic['readers'] }}</span>
                            </div>
                        </div>

                        <a href="{{ route('chapters.show', ['slug' => $comic['slug'], 'chapter' => $comic['latest_chapter']['number']]) }}" class="btn btn-primary mt-5 w-full rounded-2xl">
                            Buka Update Terbaru
                        </a>
                    </div>
                </div>
            </div>
        </article>

        <section class="grid gap-5 xl:grid-cols-[minmax(0,1fr)_320px]">
            <div class="space-y-5">
                <div class="card border border-base-300/70 bg-base-100/60 shadow-lg">
                    <div class="card-body gap-5">
                        <div class="flex flex-col gap-4 md:flex-row md:items-end md:justify-between">
                            <div>
                                <div class="text-xs uppercase tracking-[0.28em] text-base-content/45">Chapter hub</div>
                                <h2 class="mt-2 text-2xl font-semibold sm:text-3xl">Daftar chapter</h2>
                                <p class="mt-1 text-sm text-base-content/55">{{ $comic['chapter_total'] }} chapter siap dibaca, urut dari yang terbaru.</p>
                            </div>

                            <div class="flex flex-wrap gap-2">
                                <span class="badge badge-primary badge-lg">{{ $comic['status'] }}</span>
                                <span class="badge badge-outline badge-lg border-base-300/70">{{ $comic['genre_line'] }}</span>
                            </div>
                        </div>

                        <div class="grid gap-3 md:grid-cols-2">
                            <a href="{{ route('chapters.show', ['slug' => $comic['slug'], 'chapter' => $comic['first_chapter']['number']]) }}" class="flex items-center justify-between rounded-[1.35rem] border border-base-300/70 bg-base-200/45 px-4 py-4 transition hover:bg-base-200/70">
                                <span>
                                    <span class="block font-semibold">Mulai baca dari awal</span>
                                    <span class="text-sm text-base-content/55">{{ $comic['first_chapter']['label'] }} · {{ $comic['first_chapter']['title'] }}</span>
                                </span>
                                <span class="badge badge-primary">Start</span>
                            </a>

                            <a href="{{ route('chapters.show', ['slug' => $comic['slug'], 'chapter' => $comic['latest_chapter']['number']]) }}" class="flex items-center justify-between rounded-[1.35rem] border border-base-300/70 bg-base-200/45 px-4 py-4 transition hover:bg-base-200/70">
                                <span>
                                    <span class="block font-semibold">Lompat ke terbaru</span>
                                    <span class="text-sm text-base-content/55">{{ $comic['latest_chapter']['label'] }} · {{ $comic['latest_chapter']['title'] }}</span>
                                </span>
                                <span class="badge badge-secondary">New</span>
                            </a>
                        </div>

                        <div class="space-y-3">
                            @foreach ($chapters as $chapter)
                                <article class="rounded-[1.5rem] border border-base-300/70 bg-base-100/65 p-4 shadow-sm transition hover:border-primary/30 hover:bg-base-100">
                                    <div class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
                                        <div class="flex min-w-0 items-start gap-4">
                                            <div class="flex h-14 w-14 shrink-0 items-center justify-center rounded-2xl bg-primary/12 text-lg font-semibold text-primary">
                                                {{ str_pad((string) $chapter['number'], 2, '0', STR_PAD_LEFT) }}
                                            </div>

                                            <div class="min-w-0">
                                                <div class="flex flex-wrap items-center gap-2 text-xs text-base-content/50">
                                                    <span class="badge badge-outline border-base-300/70">{{ $chapter['label'] }}</span>
                                                    @if ($chapter['is_latest'])
                                                        <span class="badge badge-secondary">Baru</span>
                                                    @endif
                                                    <span>{{ $chapter['release_label'] }}</span>
                                                </div>

                                                <h3 class="mt-2 text-lg font-semibold">{{ $chapter['title'] }}</h3>
                                                <p class="mt-1 text-sm text-base-content/60">{{ $chapter['page_count'] }} halaman · {{ $chapter['reading_time'] }}</p>
                                            </div>
                                        </div>

                                        <div class="flex items-center gap-3 md:shrink-0">
                                            <div class="hidden text-right text-xs text-base-content/50 sm:block">
                                                <div>{{ $chapter['page_count'] }} pages</div>
                                                <div>{{ $chapter['reading_time'] }}</div>
                                            </div>
                                            <a href="{{ route('chapters.show', ['slug' => $comic['slug'], 'chapter' => $chapter['number']]) }}" class="btn btn-primary btn-sm rounded-2xl">
                                                Baca
                                            </a>
                                        </div>
                                    </div>
                                </article>
                            @endforeach
                        </div>
                    </div>
                </div>
            </div>

            <aside class="space-y-4 xl:sticky xl:top-24 xl:h-fit">
                <div class="card border border-base-300/70 bg-base-100/60 shadow-lg">
                    <div class="card-body gap-4">
                        <div>
                            <div class="text-xs uppercase tracking-[0.28em] text-base-content/45">Tentang seri</div>
                            <h2 class="mt-2 text-2xl font-semibold">Ringkasan</h2>
                            <p class="mt-3 text-sm leading-7 text-base-content/65">{{ $comic['tagline'] }}</p>
                        </div>

                        <div class="grid gap-3">
                            <div class="rounded-2xl border border-base-300/70 bg-base-200/40 p-4">
                                <div class="text-xs uppercase tracking-[0.25em] text-base-content/45">Author</div>
                                <div class="mt-1 font-semibold">{{ $comic['author'] }}</div>
                            </div>
                            <div class="rounded-2xl border border-base-300/70 bg-base-200/40 p-4">
                                <div class="text-xs uppercase tracking-[0.25em] text-base-content/45">Artist</div>
                                <div class="mt-1 font-semibold">{{ $comic['artist'] }}</div>
                            </div>
                            <div class="grid grid-cols-2 gap-3">
                                <div class="rounded-2xl border border-base-300/70 bg-base-200/40 p-4">
                                    <div class="text-xs uppercase tracking-[0.25em] text-base-content/45">Rating</div>
                                    <div class="mt-1 font-semibold">{{ $comic['rating'] }}/5</div>
                                </div>
                                <div class="rounded-2xl border border-base-300/70 bg-base-200/40 p-4">
                                    <div class="text-xs uppercase tracking-[0.25em] text-base-content/45">Year</div>
                                    <div class="mt-1 font-semibold">{{ $comic['year'] }}</div>
                                </div>
                            </div>
                            <div class="rounded-2xl border border-base-300/70 bg-base-200/40 p-4">
                                <div class="text-xs uppercase tracking-[0.25em] text-base-content/45">Genre</div>
                                <div class="mt-2 flex flex-wrap gap-2">
                                    @foreach ($comic['genres'] as $genre)
                                        <span class="badge badge-outline border-base-300/70">{{ $genre }}</span>
                                    @endforeach
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                @if (! empty($comic['features']))
                    <div class="card border border-base-300/70 bg-base-100/60 shadow-lg">
                        <div class="card-body">
                            <h2 class="text-2xl font-semibold">Highlight</h2>
                            <div class="mt-1 flex flex-wrap gap-2">
                                @foreach ($comic['features'] as $feature)
                                    <span class="badge badge-primary badge-outline border-primary/30">{{ $feature }}</span>
                                @endforeach
                            </div>
                        </div>
                    </div>
                @endif

                <div class="card border border-base-300/70 bg-base-100/60 shadow-lg">
                    <div class="card-body">
                        <div class="flex items-center justify-between gap-3">
                            <h2 class="text-2xl font-semibold">Serupa</h2>
                            <a href="{{ route('comics.index') }}" class="link link-hover text-sm">Lihat semua</a>
                        </div>

                        <div class="space-y-3">
                            @foreach ($related as $item)
                                <div class="flex items-center gap-3 rounded-2xl border border-base-300/70 bg-base-200/40 p-3">
                                    <img src="{{ $item['cover'] }}" alt="{{ $item['title'] }} cover" class="w-14 rounded-xl border border-base-300/70" loading="lazy" decoding="async">
                                    <div class="min-w-0 flex-1">
                                        <div class="truncate font-semibold">{{ $item['title'] }}</div>
                                        <div class="text-xs text-base-content/50">{{ implode(' • ', array_slice($item['genres'], 0, 2)) }}</div>
                                    </div>
                                    <a href="{{ route('comics.show', $item['slug']) }}" class="btn btn-ghost btn-xs rounded-xl border border-base-300/70">Buka</a>
                                </div>
                            @endforeach
                        </div>
                    </div>
                </div>
            </aside>
        </section>
    </section>
@endsection
